# Agent Browser 核心规则

AI 驱动的浏览器自动化顶层指导原则。

## 核心准则

- **选择器鲁棒性**: 优先使用 `[data-testid]`、`[aria-label]`、`#id`，避免脆弱的 CSS 类链
- **验证优先**: 每次操作后截图验证，失败时自动重试一次
- **渐进式脚本**: 从简单导航开始，逐步增加等待、断言和错误处理
- **会话隔离**: 无关任务使用独立浏览器上下文，避免状态污染
- **尊重目标网站**: 遵守 robots.txt，设置合理请求间隔

## 常用指令

- `agent-browser goto <url>`: 导航到 URL
- `agent-browser click <selector>`: 点击元素
- `agent-browser type <selector> <text>`: 输入文本
- `agent-browser extract <selector>`: 提取内容
- `agent-browser screenshot [path]`: 截图
- `agent-browser evaluate <script>`: 执行 JS

## 安全红线

- 永远不要在未授权的情况下对生产网站执行自动化操作
- 不要在自动化脚本中硬编码凭证——使用环境变量
- 涉及用户数据的操作必须经过明确授权
