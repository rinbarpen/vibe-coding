---
description: 学术论文写作规则：引用规范、逻辑连贯性、LaTeX/Typst 代码风格与学术语气
globs: ["**/*.{tex,bib,typ,md}"]
alwaysApply: false
---

# Vibe Coding Academic Writing Rules

针对学术论文写作场景的 AI 指令，旨在提升论文质量、逻辑严密性和格式规范。

## 1. 学术语气与风格 (Academic Tone)
- **正式性**: 使用正式、客观的学术语言。避免缩写（如 "don't" -> "do not"）、俚语或过于口语化的表达。
- **准确性**: 术语使用必须准确且前后一致。首次使用缩写时必须给出全称。
- **被动语态**: 根据学科习惯合理使用被动语态或 "We"（避免使用 "I"）。

## 2. 引用与参考文献 (Citations & Bibliography)
- **可追溯性**: 所有核心观点、数据和方法必须有引用支持。
- **引用检查**: 每次修改后，检查 `\cite` 或 `@cite` 标签是否在 `.bib` 文件中有对应条目。
- **格式一致性**: 确保所有参考文献条目信息完整（作者、年份、标题、期刊/会议、卷号、页码）。

## 3. 逻辑与结构 (Logic & Structure)
- **段落一致性**: 每个段落应有一个明确的中心句 (Topic Sentence)，并围绕该中心展开。
- **过渡衔接**: 段落之间、章节之间应有自然的逻辑过渡。
- **数学严谨性**: 确保所有数学符号、公式编号正确，且所有变量在首次出现时均已定义。

## 4. LaTeX / Typst 规范
- **代码整洁**: 保持源代码缩进整齐，合理使用注释。
- **交叉引用**: 优先使用 `\label` 和 `\ref`（或 Typst 的 `<label>` 和 `@label`），避免手动硬编码编号。
- **图表规范**: 图表必须有 Caption，且在正文中必须至少被提及一次。

## 5. 验证流程 (Verification)
- **编译检查**: 每次重大修改后，运行编译命令（如 `latexmk` 或 `typst compile`）确保无报错。
- **拼写与语法**: 提交前运行 `vibe-spell-check` 或类似工具。
- **字数统计**: 监控字数或页数，确保符合目标期刊/会议的要求。
