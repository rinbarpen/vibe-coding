# Excel 电子表格操作核心规则

针对电子表格创建、数据分析和自动化的顶层指导原则。

## 核心准则

- **Python Only**: 所有 Excel 操作必须通过 openpyxl 完成，严禁直接打开文件手动修改。
- **Copy-First**: 修改前必须先 `cp original.xlsx working.xlsx`，在副本上操作。
- **验证后替换**: 每次修改后必须验证 working copy 结构完整，再替换原始文件。
- **Git 分支隔离**: 每次操作在独立分支 `excel/<task>` 上进行，支持回滚。
- **自动备份**: 原始文件被替换前，存入 `_backups/original-{timestamp}.xlsx`。
- **数据不可变**: 原始数据放在独立的 "Raw" sheet 中，永远不直接修改。

## 常用指令

- `vibe-excel-create <name> <columns>`: 创建新 workbook
- `vibe-excel-import <file> <format>`: 从 CSV/JSON 导入数据
- `vibe-excel-validate <file>`: 验证数据结构完整性
- `vibe-excel-analyze <file>`: 运行统计分析
- `vibe-excel-pivot <file> <rows> <values>`: 生成数据透视表
- `vibe-excel-chart <file> <type> <range>`: 创建图表
- `vibe-excel-export <file> <format>`: 导出为 CSV/JSON/PDF

## 详细规则

更多关于数据清洗、公式标准、图表设计及性能优化的详细规则，请参阅：
`rules/excel-analysis.mdc`
`rules/excel-python-safety.mdc`
