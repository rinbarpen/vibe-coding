# 演示文稿创建核心规则

针对演示文稿创建、模板管理和自动化的顶层指导原则。

## 核心准则

- **ppt-master 优先**: 使用 ppt-master 管理幻灯片母版，确保整个演示文稿的风格一致性。
- **Copy-First**: 修改 pptx 文件前必须先 `cp original.pptx working.pptx`，在副本上操作。
- **验证后替换**: 每次修改后必须验证 working copy 结构完整（slides、layouts、images），再替换原始文件。
- **Git 分支隔离**: 每次操作在独立分支 `ppt/<task>` 上进行，支持回滚。
- **自动备份**: 原始文件被替换前，存入 `_backups/original-{timestamp}.pptx`。
- **一页一信息**: 每张幻灯片只传达一个核心信息，最多 6 个要点或 30 个词。
- **视觉一致性**: 整个演示文稿使用同一个幻灯片母版，颜色不超过 3 种，字体不超过 2 种。

## 常用指令

- `ppt-master init <name>`: 用模板初始化新演示文稿
- `ppt-master apply <template>`: 应用幻灯片母版
- `ppt-master export <format>`: 导出为 PDF/图片
- `vibe-ppt-create <title> <slides>`: 从大纲创建演示文稿
- `vibe-ppt-template <file> <template>`: 应用模板到已有文稿
- `vibe-ppt-animate <file>`: 标准化动画和过渡效果
- `vibe-ppt-notes <file>`: 管理演讲者备注
- `vibe-ppt-export <file> <format>`: 导出为多种格式
- `vibe-ppt-validate <file>`: 验证演示文稿结构完整性

## 详细规则

更多关于幻灯片设计、模板管理、动画使用及数据安全的详细规则，请参阅：
`rules/ppt-creation.mdc`
`rules/ppt-safety.mdc`
