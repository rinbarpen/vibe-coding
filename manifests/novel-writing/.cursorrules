# Cursor Rules

## 创作风格与约束

- **人设一致性**: 在生成对话或描写角色动作时，必须先读取 `characters/` 下的相关设定，严禁 OOC。
- **叙述视角**: 严格遵守当前章节设定的 POV，除非作者明确要求，否则不得随意切换视角。
- **描写风格**: 优先使用“展示而非叙述 (Show, Don't Tell)”的技巧。避免平铺直叙，多通过环境、动作、细节来体现情感。
- **语言禁忌**: 避免使用现代网络用语（除非是现代背景小说），保持文学性。

## 项目结构规范

- **章节命名**: 章节文件建议命名为 `chapters/CH001_标题.md`，方便排序。
- **设定同步**: 修改剧情导致设定变更时，必须同步更新 `outline/` 或 `world/` 下的相关文档。

## AI 交互行为

- **主动建议**: 当检测到剧情可能出现逻辑漏洞（如角色瞬间移动、力量等级崩坏）时，AI 应主动发出提醒。
- **多方案提供**: 在进行剧情头脑风暴时，至少提供 3 个不同走向的方案（稳健、反转、极端）。
- **长文本处理**: 编写长章节时，建议先生成详细的小节大纲，确认后再逐段填充。

## 验证与检查

- **字数达标**: 确保生成的章节内容充实，不应通过大量废话凑字数。
- **逻辑闭环**: 提交新章节前，AI 应自检该章节是否完成了预定的剧情目标。
