---
description: 小说创作的质量门禁 — 章节完成前的最终检查清单
globs: ["**/*"]
alwaysApply: false
---

# Quality Gate Rules

章节完成或提交前的最终检查清单：

## 必须通过的检查

- [ ] 章节完成了预定的剧情目标
- [ ] 内容充实，没有废话凑字数
- [ ] 字数达标（3000-5000字/章 为参考标准）
- [ ] 与 `outline/` 中的规划一致
- [ ] 与 `world/` 中的世界观设定无冲突
- [ ] 角色言行未 OOC
- [ ] 时间线合理
- [ ] 语言风格与全书基调一致

## 循环校验

1. 新章节写入 `chapters/` 后 → 自动触发 `vibe-check-logic`
2. 发现问题 → 修复 → 重新检查 → 通过后继续
3. 更新 `CLAUDE.md` 中的进度状态

## 通过标准

- 所有检查项标记 ✅
- `vibe-check-logic --full` 无报错
- `vibe-progress` 进度更新正确