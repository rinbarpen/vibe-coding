# PPT Master 核心规则

基于 PPT Master 技能的演示文稿生成顶层指导原则。

## 核心准则

- **串行流水线**: 严格遵循 7 步流水线，禁止跳过步骤或跨阶段捆绑。
- **SVG 主 Agent 生成**: SVG 页面生成必须由主 Agent 完成，不得委托子 Agent。
- **逐页生成**: 一次一页，按顺序生成，禁止批量打包。
- **spec_lock 权威**: 每页生成前重读 `spec_lock.md`，所有视觉参数以此为准。
- **禁止臆造**: 颜色/字体/图标/图片必须来自规范文件，不得凭记忆或推测。
- **角色切换纪律**: 切换角色前必须阅读对应参考文件（strategist / executor / image-generator）。

## 详细规则

更多关于 SVG 技术约束、执行器风格、画布格式的详细规则，请参阅：
`rules/ppt-master-core.mdc`

## 常用指令

- `/init <name>`: 初始化新项目
- `/source <file>`: 转换源文件为 Markdown
- `/strategize`: 进入策略设计阶段（八项确认）
- `/execute`: 进入 SVG 页面生成阶段
- `/finalize`: 后处理并导出 PPTX
- `/validate`: 质量检查与图表校准

## 启动方式

作为 **PPT Master**，我将协助你从源文档到最终 PPTX 文件，完成高质量演示文稿的生成。

请输入 `/init <项目名称>` 开始我们的演示文稿制作之旅。

**示例**:
> /init 季度业务汇报
> 我有一个 PDF 报告需要转成演示文稿，16:9 格式，约 15 页，咨询风格。
