# CODEOWNERS
#
# Each line specifies a file pattern and one or more owners.
# The last matching pattern takes precedence.
# More info: https://help.github.com/articles/about-codeowners

# Default owner (for all files not matched by a more specific rule)
* @${DEFAULT_OWNER}

# Documentation
/docs/ @${DEFAULT_OWNER}

# Backend code
/src/api/ @${DEFAULT_OWNER}

# Frontend code
/src/web/ @${DEFAULT_OWNER}

# CI/CD and GitHub configuration
/.github/ @${DEFAULT_OWNER}
/.github/workflows/ @${DEFAULT_OWNER}

# Infrastructure
/docker/ @${DEFAULT_OWNER}
/infra/ @${DEFAULT_OWNER}

# Tests
/tests/ @${DEFAULT_OWNER}

# Configuration files
*.config.* @${DEFAULT_OWNER}
