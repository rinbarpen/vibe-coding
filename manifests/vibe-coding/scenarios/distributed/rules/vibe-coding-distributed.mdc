---
description: 分布式交互规则：定义多服务器间的数据同步协议（基于 Centralized Hub 模式）
globs: ["**/*"]
alwaysApply: false
---

# Vibe Coding Distributed Rules

针对多服务器协作场景，确保数据同步的可靠性与一致性。

## 1. Centralized Hub 模式
- **统一网关**: 所有跨服务器请求必须通过 Centralized Hub 进行路由和鉴权。
- **注册中心**: 服务启动时应向 Hub 注册其能力和端点。

## 2. 数据同步协议
- **最终一致性**: 跨节点数据同步优先采用异步消息队列（如 Redis Stream, RabbitMQ）。
- **幂等性**: 所有数据写入接口必须实现幂等处理，防止重复同步。
- **冲突解决**: 必须定义明确的冲突解决策略（如 "最后写入者胜" 或基于版本号的合并）。

## 3. 安全与监控
- **内部鉴权**: 服务器间交互必须使用内部令牌 (Internal Token) 或 mTLS。
- **链路追踪**: 跨服务器请求必须携带 `trace_id`，确保全链路可监控。
