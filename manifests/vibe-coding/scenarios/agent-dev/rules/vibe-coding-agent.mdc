---
description: Agent 开发核心规则：自主循环、工具调用、状态管理与调试规范
globs: ["**/*"]
alwaysApply: false
---

# Vibe Coding Agent Development Rules

针对 AI Agent 场景的开发规范，确保 Agent 的可靠性、可观测性与安全性。

## 1. 自主循环 (Agentic Loop)
- **状态追踪**: 必须实现明确的状态机或日志记录，追踪 Agent 的思考 (Thought)、行动 (Action) 与观察 (Observation)。
- **终止条件**: 严禁无限循环，必须设置最大迭代次数 (Max Iterations) 或超时机制。
- **错误恢复**: 实现重试逻辑与优雅降级，当工具调用失败时，Agent 应能尝试其他策略。

## 2. 工具调用 (Tool Use)
- **模式定义**: 工具的输入输出 Schema 必须严格定义（如 JSON Schema）。
- **副作用控制**: 涉及删除、修改、支付等高危操作时，必须实现人工确认 (Human-in-the-loop) 或严格的沙箱隔离。
- **文档化**: 每个工具必须有清晰的描述，指导 AI 如何正确调用。

## 3. 记忆与上下文 (Memory & Context)
- **短期记忆**: 使用窗口机制或摘要机制管理长对话上下文。
- **长期记忆**: 推荐使用向量数据库 (RAG) 或持久化存储来检索历史知识。

## 4. 调试与评估
- **Trace 日志**: 必须记录完整的 Prompt 链和 LLM 原始响应，便于回溯调试。
- **单元测试**: 针对工具逻辑进行 100% 覆盖的单元测试。
- **集成测试**: 使用模拟 LLM (Mock LLM) 进行端到端流程验证。
