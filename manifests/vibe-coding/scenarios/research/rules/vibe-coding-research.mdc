---
description: 科研训练核心规则：实验记录、绘图规范、文献管理与论文协作
globs: ["**/*"]
alwaysApply: false
---

# Vibe Coding Research Rules

针对科研训练与论文写作场景的规范，强调实验的可重复性与学术严谨性。

## 1. 实验记录 (Experiment Tracking)
- **可重复性**: 每次实验必须记录随机种子 (Seed)、超参数、硬件环境与软件版本。
- **自动记录**: 推荐使用 Weights & Biases (W&B) 或 MLflow 自动记录实验指标。
- **日志规范**: 实验输出必须包含时间戳和关键参数摘要。

## 2. 数据与模型版本控制
- **数据版本**: 使用 DVC 或 Git LFS 管理大型数据集和模型权重。
- **检查点 (Checkpoint)**: 训练过程中必须定期保存 Checkpoint，并记录对应的验证集指标。

## 3. 绘图与可视化 (Visualization)
- **语言限制**: **Matplotlib/Seaborn 绘图中的所有文字（坐标轴、图例、标题）必须使用英语。**
- **格式要求**: 论文配图推荐保存为矢量格式（如 `.pdf`, `.svg`, `.eps`）。
- **配色方案**: 使用色盲友好且学术规范的配色方案。

## 4. 论文写作辅助
- **LaTeX 协作**: 保持 `.tex` 文件结构清晰，公式必须符合学术规范。
- **文献引用**: 确保 `references.bib` 格式正确，引用键 (Citation Key) 命名统一。
- **AI 辅助**: 使用 AI 进行润色时，必须保持原意，严禁捏造事实或虚构文献。
