---
description: SaaS 核心规则：租户隔离、订阅逻辑、多租户架构规范
globs: ["**/*"]
alwaysApply: false
---

# Vibe Coding SaaS Rules

针对 SaaS 场景的开发规范，确保多租户安全与架构可扩展性。

## 1. 租户隔离 (Tenant Isolation)
- **数据隔离**: 所有数据库查询必须包含 `tenant_id` 过滤，严禁跨租户访问。
- **配置隔离**: 租户特定的配置（如域名、Logo、功能开关）应从租户上下文中读取。
- **中间件**: 必须实现租户识别中间件，从 Header、Domain 或 JWT 中提取 `tenant_id`。

## 2. 订阅与权限
- **功能开关**: 使用 Feature Flags 控制不同订阅等级的功能可用性。
- **配额限制**: 关键资源（如存储、API 调用次数）必须实现配额检查逻辑。

## 3. 架构规范
- **前后端分离**: 必须通过 RESTful 或 GraphQL API 进行交互，前端不直接访问数据库。
- **无状态设计**: 服务端应保持无状态，以支持水平扩展。
