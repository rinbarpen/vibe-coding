---
description: GitHub 协作与自动化规范：PR、CI、Issue 管理
globs: ["**/*"]
alwaysApply: true
---

# GitHub Collaboration & Automation

本规则定义了在 GitHub 托管平台上进行协作和自动化执行的标准流程。

## 1. Pull Request (PR) 流程
- **创建 PR**: 必须使用 `gh pr create` 命令。
- **描述规范**: 
  - 使用 `--fill` 自动提取 commit 信息。
  - 必须包含 `## Summary` (变更摘要) 和 `## Test Plan` (测试计划)。
- **自动化检查**: PR 创建后，必须调用 `ci-watcher` 或运行 `gh pr checks` 监控 CI 状态。

## 2. 自动化执行 (Automation)
- **Post-Commit**: 每次 commit 后运行 `vibe-post-commit.sh` 同步文档和状态。
- **CI 修复**: 如果 CI 失败，必须启动 `ci-watcher` 或 `fix-ci` subagent 分析日志并应用修复。
- **依赖更新**: 优先使用 `gh` CLI 触发或管理 Dependabot/Renovate PR。

## 3. GitHub CLI (`gh`) 常用指令
- `gh pr status`: 查看当前 PR 状态。
- `gh pr checks`: 查看 CI 检查结果。
- `gh pr view --web`: 在浏览器中打开当前 PR。
- `gh run list`: 查看最近的 GitHub Actions 运行记录。

## 4. 协作约定
- **代码评审**: 提交 PR 前，必须先调用 `code-reviewer` 进行本地预审。
- **分支管理**: 遵循 `feature/`, `fix/`, `docs/` 等命名规范。
